# Local Development

Development supports two modes:

1. **Connected:** Connects to a real Supabase project and uses a real Gemini API key.
2. **Local Test / Mock:** Uses a local Supabase instance, mocked MediaRecorder, and mocked Gemini API responses (for isolated testing).